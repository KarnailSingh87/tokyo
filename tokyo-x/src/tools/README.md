# Tools (Phase 5)

Safe PC tool implementations behind the schema defined in `src/core/tool-schema.ts`: file ops sandboxed to workspace root, terminal exec with allow/deny filters + timeout, browser automation, screen read/capture. Placeholder until Phase 5.
