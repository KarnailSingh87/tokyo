# Voice (Phase 3)

Mic capture pipeline, STT placeholder interface (provider pluggable), ElevenLabs TTS with voice presets from config, and safe fallbacks when keys are missing. Placeholder until Phase 3.
